basic project 的提升版吧，也没啥提升的；
主要包括：
1.创建了多个接口，便于实现多人聊天，服务器带转发功能且服务器也可参与发言不比一直阻塞等待；
2.调用了线程，了解了一点点多线程的知识，消息队列啥的；
3.了解了select(),poll(),epoll()；
4.加深了对bind(),accept(),connect(),recv(),send()的理解；
5.了解了c++事件;
6.TCP...;

这个聊天室BUG还是很多，且由于是在Window写的，与Linux并不兼容，环境要搭配好才能跑起来(链接好麻烦啊)，改天再用Linux写；
收获还是蛮大的==；



